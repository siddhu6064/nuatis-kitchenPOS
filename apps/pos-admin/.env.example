PORT=3001
NEXTAUTH_URL=http://localhost:3001
# openssl rand -base64 32
NEXTAUTH_SECRET=
AUTH_SECRET=

# pos-api base URL (server-to-server calls from Next.js)
POS_API_URL=http://localhost:3002

# Supabase (needed for Auth.js adapter if used)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Must match pos-api POS_JWT_SECRET — admin issues JWTs pos-api will verify
POS_JWT_SECRET=
